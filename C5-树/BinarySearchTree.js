class Node{
    constructor(key) {
        this.key = key; //节点值，键（key）是树相关的术语中对节点的称呼
        this.left = left; //左侧子节点引用
        this.right = right; //右侧子节点引用
    }
}
class BinarySearchTree{
    constructor(compareFn = defaultCompare) {
        
    }
}